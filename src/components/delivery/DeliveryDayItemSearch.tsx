import React from 'react';
import MenuItem from "../../models/MenuItemModel";

interface Props {
    itemSelected: (item: MenuItem) => void
}

const DeliveryDayItemSearch = (): React.ReactElement => {

    return (
        <div></div>
    )
}